public_key : s007sio79f708c25a23ed367610facc14035adc7ba4b1bfa9252ef55c6c24f1b9b03abd
type       : "src"
name       : "portcullis"
app        : public_key::type::name

mod app::etc {

    summon data Number
        stable | U32(u32)
        sketch | U64(u64)
    portcullis {
        | produce [mapper] -> [Number] {
            produce lazy :: (i: u32) -> [Number] {
                wait(10)
                Number::U32(i) | produce(i + 1)
            }
            produce(0u32)
        }
    }
    portcullis {
        skip
    }
}

// print("ok") // expect: ok
